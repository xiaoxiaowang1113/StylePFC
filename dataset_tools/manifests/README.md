# Released curation manifests

This directory stores the non-image manifests required to reconstruct the
released StyleFamilyBench selection. See the repository-level `DATASET.md` for
the complete workflow.
